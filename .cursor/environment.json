{
  "$schema": "https://www.cursor.com/schemas/environment.schema.json",
  "user": "ubuntu",
  "install": "python -c \"import pandas, matplotlib, pytest\"",
  "build": {
    "dockerfile": "../Dockerfile",
    "context": ".."
  }
}
